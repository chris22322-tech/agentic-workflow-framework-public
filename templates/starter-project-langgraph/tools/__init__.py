"""
Tools Package
=============
Each tool module in this package wraps a single external data source
(API, database, file system, etc.). One tool per file — each with its
own error handling.

Methodology reference:
  - Stage 3: Scope → Data Inventory and Integration Requirements
  - Stage 5: Build → "One tool per file" convention
"""
