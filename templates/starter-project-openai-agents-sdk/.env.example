# OpenAI API key — required for the agent's reasoning loop
OPENAI_API_KEY=sk-proj-...

# Optional: override default model
# OPENAI_MODEL=gpt-4o-mini

# Data source credentials (one per data source in your Stage 3 Data Inventory)
EXAMPLE_API_URL=https://api.example.com
EXAMPLE_API_KEY=

# Rate limiting / observability (optional)
# OPENAI_LOG_LEVEL=info
